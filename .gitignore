node_modules
.DS_Store
dist
dist-ssr
*.local
dist
yarn-error.log
.media

# secrets
.env
.env.local
.env.*.local
.env.*
!.env.example

# packaged extension / signing
*.zxp
*.p12

# claude code local overrides
.claude/settings.local.json

# The deobfuscated 1.4.0 reference dump lived here until parity was reached.
# It is gone from the tree now and kept outside the repo. The rule stays so a
# stray copy - which still holds the old plaintext endpoint and shared key -
# cannot be committed to this public repo by accident.
AutoEditRestored/