node_modules
.DS_Store
dist
dist-ssr
*.local
dist
yarn-error.log
.media

# secrets
.env
.env.local
.env.*.local
.env.*
!.env.example

# packaged extension / signing
*.zxp
*.p12

# claude code local overrides
.claude/settings.local.json

# deobfuscated reference dump of the shipped build - not maintained, to be
# deleted once src/ reaches feature parity. Contains a plaintext endpoint
# and shared key; must never be committed to this public repo.
AutoEditRestored/